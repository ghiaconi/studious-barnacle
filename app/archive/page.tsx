import TokenDashboard from "@/components/Token";

const Page = () => {
  return (
    <div>
      here is the archive page, this will have the dashboard tokens, and
      <TokenDashboard />
    </div>
  );
};

export default Page;
